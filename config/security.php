<?php

return [

    /*
    |--------------------------------------------------------------------------
    | API IP Allowlist
    |--------------------------------------------------------------------------
    |
    | รายการ IP ที่อนุญาตให้เข้าถึง API สำคัญ (ใช้กับ middleware 'ip.allow')
    | ตั้งค่าผ่าน .env เช่น: ALLOWED_API_IPS=192.168.1.136,192.168.1.137
    | ถ้าปล่อยว่าง = ไม่จำกัด IP
    |
    */

    'allowed_ips' => array_values(array_filter(array_map(
        'trim',
        explode(',', env('ALLOWED_API_IPS', '192.168.1.136'))
    ))),

];
